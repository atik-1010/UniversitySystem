services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: university_sql

    environment:
      ACCEPT_EULA: "Y"
      SA_PASSWORD: "Password@123"

    ports:
      - "1433:1433"